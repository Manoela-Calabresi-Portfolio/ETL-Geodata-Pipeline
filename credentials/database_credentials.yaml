# Database Credentials - YOUR ACTUAL CREDENTIALS
# NEVER commit this file to git!
# Keep this file secure and private!

database:
  # PostgreSQL superuser (for setup)
  postgres:
    host: "localhost"
    port: 5432
    user: "postgres"
    password: "Acmj172-"
    
  # ETL Pipeline database (will be created)
  etl_pipeline:
    host: "localhost"
    port: 5432
    database: "etl_geodata_pipeline"
    user: "etl_user"
    password: "etl_secure_password_2024"
    
  # Connection settings
  connection:
    max_connections: 10
    connection_timeout: 30
    query_timeout: 300
    
  # Security settings
  security:
    ssl_mode: "prefer"
    enable_ssl: false
